
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model StockItem {
  productId String @id 
  quantity  Int    @default(0)

  updatedAt DateTime @updatedAt

  @@map("stock_items")
}
